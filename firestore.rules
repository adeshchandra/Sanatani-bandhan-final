rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT OVERRIDE: Since Firebase Auth cannot be configured due to IAM permission issues,
    // we are temporarily opening the rules so the app can function in a local mocked-auth state.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
