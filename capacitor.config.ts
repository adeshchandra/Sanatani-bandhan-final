import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sanatanibandhan.app',
  appName: 'Sanatani Bandhan',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
